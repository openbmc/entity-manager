templates = [
    'IntelFanConnector.template',
]

foreach t : templates
    install_data(
        t,
        install_dir: join_paths(
            packagedir,
            'overlay_templates'
        )
    )
endforeach
