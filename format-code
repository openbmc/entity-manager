#!/bin/sh

set -euo

set -x

if [[ -v 1 ]]; then
    cd "$1";
fi;

if [[ -v WORKSPACE ]]; then
    $WORKSPACE/format-code.sh;
fi;
python3 $WORKSPACE/$UNIT_TEST_PKG/scripts/autojson.py $WORKSPACE/$UNIT_TEST_PKG/configurations
